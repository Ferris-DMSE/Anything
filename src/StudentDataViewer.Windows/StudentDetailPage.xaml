<Page
    x:Class="StudentDataViewer.Windows.StudentDetailPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:StudentDataViewer.Windows"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Loaded="StudentDetailPage_OnLoaded"
    Unloaded="StudentDetailPage_OnUnloaded">

    <Page.Transitions>
        <TransitionCollection>
            <NavigationThemeTransition/>
        </TransitionCollection>
    </Page.Transitions>

    <StackPanel x:Name="RootPanel" x:DeferLoadStrategy="Lazy" Orientation="Vertical">
        <TextBlock
            Margin="8,0"
            Style="{ThemeResource TitleTextBlockStyle}"
            HorizontalAlignment="Left"
            Text="{x:Bind Student.Student.FirstName}"/>

        <TextBlock
            Margin="8,0"
            HorizontalAlignment="Left"
            Text="{x:Bind Student.Courses.Count}"/>
    </StackPanel>
</Page>
